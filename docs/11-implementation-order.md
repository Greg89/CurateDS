# Implementation Order

Foundation -> First Collection -> Collection Workspace -> Browse -> Insights -> Customization -> Showcase

For the V2 implementation sequence, use [V2 Roadmap](15-v2-roadmap.md). The existing backend and domain behavior should be preserved while the web experience is rebuilt around the collection workspace.
