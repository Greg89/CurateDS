# End-To-End Tests

This workspace contains browser-level smoke and workflow coverage using `playwright`.

Use these tests for a small number of high-value scenarios, such as:

- app shell booting in a real browser
- routed navigation across the client
- critical end-to-end collection workflows against the running stack
