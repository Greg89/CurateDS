# Web Unit Tests

This workspace contains fast client-side tests for the React app using:

- `vitest`
- `@testing-library/react`
- `jsdom`
- `msw`

Use these tests for TDD around:

- routed screen rendering
- component and form behavior
- loading, empty, success, and error states
- client workflows that should not require Docker to validate
